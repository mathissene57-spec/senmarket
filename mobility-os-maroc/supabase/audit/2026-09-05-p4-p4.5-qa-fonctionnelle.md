# P4/P4.5 : QA fonctionnelle des 3 apps + multi-tenant etendu

## Blocage confirme : pas d'acces reseau sortant vers Supabase/Vercel depuis ce bac a sable

Deja signale au demarrage de ce chantier (tache #6, "bloque : reseau
sandbox"), reconfirme explicitement avant de commencer P4 :

```
curl https://hfybtcyhhzgwirtqdqmt.supabase.co/rest/v1/  -> CONNECT tunnel failed, response 403
curl https://vercel.com                                  -> CONNECT tunnel failed, response 403
```

La politique reseau de cet environnement (proxy de sortie de l'agent)
bloque explicitement ces deux domaines ("organization policy"). Consequence
directe : **aucun test de bout en bout via navigateur (Playwright) contre
les 3 apps reellement deployees (passager/chauffeur/dashboard) n'est
possible ici** -- ni contre le webapp Vercel, ni contre le projet Supabase
reel, meme en local (`npm run dev` fonctionne, mais tout appel
`supabase.rpc(...)` depuis le navigateur echouerait de la meme maniere,
puisque c'est la meme restriction reseau qui s'applique).

Ce n'est pas contournable depuis cette session : ni en changeant d'outil
(Playwright utilise le meme reseau que curl), ni en testant "en local"
(le blocage est sur le domaine distant, pas sur le serveur local).

## Ce qui a ete fait a la place, dans les limites de cet environnement

**1. Couverture multi-tenant etendue** : deja assuree par la suite
`supabase/tests/regression.sql`, executee integralement contre le vrai
projet Supabase a chaque phase de ce chantier (derniere execution complete :
voir commit de P3/L-1). La matrice dediee (23 cas : branding, tarifs,
configuration, chauffeurs, courses, evenements, administration, carre
complet A->A/B->B autorises, A->B/B->A/X->A interdits) couvre exactement
ce que "P4.5 multi-tenant etendu" demande, au niveau RPC/RLS -- la couche
qui porte reellement l'isolation entre operateurs. Ceci est considere
suffisant pour clore la partie "isolation multi-tenant" de P4.5.

**2. Revue de code de la remontee d'erreurs cote frontend** (verifie sans
navigateur, par lecture directe du code) : les deux nouveaux messages
d'erreur introduits en P2/P3 (H-3 "Vous avez deja une course active..." et
L-1 "Service temporairement indisponible...") remontent correctement a
l'utilisateur, car `passager/page.tsx` et `chauffeur/page.tsx` affichent
deja systematiquement `error.message` brut renvoye par chaque RPC
(`otpErreur`/`erreur`, cf. lignes ~342/398 de `passager/page.tsx` et
~239/248 de `chauffeur/page.tsx`) -- ce patron existant fonctionne pour
n'importe quel nouveau message RAISE EXCEPTION redige en francais clair
(ce qui est deja la convention de toutes les RPC de ce projet), sans
modification necessaire.

**3. Build de production verifie** : `npm run build` (webapp/) compile et
type-check sans erreur, les 10 routes (dont la nouvelle `/api/geocoder`)
sont generees correctement.

## Ce qui reste reellement non fait

Le coeur de P4 -- cliquer reellement dans les 3 apps (creer une course
passager, l'accepter cote chauffeur, suivre le dispatch temps reel, gerer
la flotte cote dashboard) contre le vrai backend -- **n'a pas pu etre
execute** dans cette session, faute d'acces reseau. C'est un vrai gap, pas
cosmetique : aucun des correctifs de ce chantier (H-3, M-1, M-2, M-3, L-1)
n'a ete verifie par un clic reel dans un navigateur reel.

**Recommandation** : avant toute presentation commerciale ou mise en
production reelle, executer une passe de QA manuelle (ou Playwright depuis
un environnement avec acces reseau complet) couvrant au minimum :
- Parcours passager complet (OTP, creation de course, annulation avec
  H-3 : verifier que le message d'erreur affiche est bien lisible).
- Parcours chauffeur complet (connexion, acceptation, transitions de
  statut, position GPS).
- Dashboard operateur (branding, zones tarifaires, flotte, cloture de
  course bloquee).
- Geocodage passager (M-2) : verifier que `/api/geocoder` renvoie bien des
  coordonnees reelles pour une adresse de Casablanca (non teste ici, cf.
  audit M-2 -- blocage reseau identique sur nominatim.openstreetmap.org).
